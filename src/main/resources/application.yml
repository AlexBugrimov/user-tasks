server:
  port: 9090
spring:
  application:
    name: user-tasks
  datasource:
    username: dev
    password: password
    url: jdbc:postgresql://localhost:5433/user_tasks?currentSchema=public
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
